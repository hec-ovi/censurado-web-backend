{{define "login"}}<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{{.Title}} | Censurado Admin</title>
<link rel="stylesheet" href="/admin/assets/admin.css">
</head>
<body class="admin-body admin-login-body">
<main class="admin-login-card">
<h1 class="admin-login-title"><span class="admin-mark">Censurado</span> Admin</h1>
{{if .Error}}<p class="admin-error" role="alert">{{.Error}}</p>{{end}}
<form class="admin-login-form" method="post" action="/admin/login">
<label class="admin-label" for="token">Operator token</label>
<input class="admin-input" id="token" name="token" type="password" autocomplete="current-password" autofocus required>
<button class="admin-btn" type="submit">Sign in</button>
</form>
</main>
</body>
</html>
{{end}}
